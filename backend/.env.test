# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=erp_user
DB_PASSWORD=erp_password
DB_NAME=erp_system_test

# JWT Configuration
JWT_SECRET=test_jwt_secret_key_for_erp_system_testing
JWT_EXPIRE=7d

# Email Configuration (Mock for testing)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=test@example.com
MAIL_PASS=test_password
MAIL_FROM=test@example.com

# Redis Configuration (Optional for testing)
REDIS_HOST=localhost
REDIS_PORT=6379

# File Upload Configuration
UPLOAD_DEST=./uploads_test
MAX_FILE_SIZE=10485760

# API Configuration
API_PREFIX=api/v1

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# AWS Configuration (Mock for testing)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test_key
AWS_SECRET_ACCESS_KEY=test_secret
S3_BUCKET_NAME=test-bucket
