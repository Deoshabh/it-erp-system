# Docker Environment Variables
# Used for Docker Compose and container orchestration

# Database Configuration
POSTGRES_DB=it-erp-backend
POSTGRES_USER=postgres
POSTGRES_PASSWORD=82e0i6QYF5nayX1dT5OFUDUvIo5LPVu53v0CJu3K4o1jB6xjgnenleenKqm4UL19

# Redis Configuration
REDIS_PASSWORD=redis-secure-password-change-in-production

# Backend Configuration
BACKEND_PORT=4101
BACKEND_NODE_ENV=production

# Frontend Configuration
FRONTEND_PORT=3003
FRONTEND_NODE_ENV=production

# Network Configuration
NETWORK_NAME=it-erp-network

# Volume Configuration
DB_DATA_VOLUME=postgres_data
REDIS_DATA_VOLUME=redis_data
UPLOADS_VOLUME=app_uploads

# Container Names
POSTGRES_CONTAINER=postgresql-database-p4ccsg0swwwkw0kgsg4gc0c8
REDIS_CONTAINER=redis-cache
BACKEND_CONTAINER=backend-app
FRONTEND_CONTAINER=frontend-app

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3

# Resource Limits
POSTGRES_MEMORY_LIMIT=1g
REDIS_MEMORY_LIMIT=512m
BACKEND_MEMORY_LIMIT=1g
FRONTEND_MEMORY_LIMIT=512m

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION=30
